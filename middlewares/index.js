const { isContactWithIdExist } = require("./isContactWithIdExist");
const { validateBody } = require("./validateBody");
const { isValidId } = require('./isValidId');

module.exports = { validateBody, isContactWithIdExist, isValidId };
